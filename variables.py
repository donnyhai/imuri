#variables

window_x_size = 500
window_size = (window_x_size, window_x_size)

point_size = (int(1/50 * window_x_size), int(1/50 * window_x_size))
tower_size = point_size
line_size = (int(1/20 * window_x_size), int(1/20 * window_x_size))